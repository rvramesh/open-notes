{
  "name": "@open-notes/electron",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix ../client && echo 'Client built successfully for Electron packaging'"
  },
  "devDependencies": {
    "electron": "^30.0.1",
    "typescript": "^5.2.2"
  }
}
